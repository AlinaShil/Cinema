server:
  port: 8000

spring:
  application:
    name: proxy-service

feature:
  newMoviesEnabled: false
  newUsersEnabled: false

movies:
  new:
    base-url: http://movies-service:8081/api/movies
  legacy:
    base-url: http://monolith:8080/api/movies

users:
  legacy:
    base-url: http://monolith:8080/api/users